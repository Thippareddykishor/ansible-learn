- name: get secrets from vault
  hosts: localhost
  connection: local
  tasks:
    - name: print
      ansible.builtin.debug:
        msg: "{{ lookup('community.hashi_vault.hashi_vault', 'secret=test/demo:username token={{vault_token}} url=http://vault.kommanuthala.store:8200') }}"